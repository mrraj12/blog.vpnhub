{
  "name": "vpnhub-netlify-cms-site",
  "version": "1.0.0",
  "description": "VPNHUB tutorial blog with Netlify + Decap CMS editing",
  "scripts": {
    "start": "eleventy --serve --config=eleventy.config.cjs",
    "build": "eleventy --config=eleventy.config.cjs"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.0"
  }
}
